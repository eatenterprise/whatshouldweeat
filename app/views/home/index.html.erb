<!-- <div class="home-div">
  <img src='https://cdn.pixabay.com/photo/2014/04/02/10/39/plate-304113_960_720.png' class="home-image" />
</div> -->
<!-- <div class="center game-instruction">
  <p>Welcome XXXX either create a new game or join a game by entering the game key.</p>
</div> -->
<div class='error-container'>
  <% if @error %>
    <% puts "I was reached, @error is #{@error}" %>
    <p><%= @error %></p>
  <% end %>

  <% flash.each do |key, value| %>
    <% puts "flash key is #{key}, flash value is #{value}" %>
    <p>test</p>
    <p><%= value %></p>
  <% end %>
</div>

<div class="index-container">

  <div class="form-container">
    <div class='game-instruction'>
      <p class="hvr-underline-from-center" id="welcome-message">Welcome! To Kafood where indecision on where to eat is solved once and for all. </p>
      <p class="hvr-underline-from-center" id="step1">1.) Collect underpants Click on the map to move the pin to the desired location.</p>
      <p class="hvr-underline-from-center dim" id="step2">2.) Choose the radius of your search</p>
      <p class="hvr-underline-from-center dim" id="step3">3.)  ????? Click create and then share the link with other people in your party. Once everyone has placed their votes click results find out where to eat.</p>
    </div>
    <div class="create-form">
      <%= form_tag rounds_path, method: :post, id: :'start-form' do |f| %>
<!--         <div><%= label_tag "Name:" %></div>
        <div><%= text_field_tag :name %></div> -->
        <div class="dim" id="radius-div">
        <%= label_tag "Radius:" %>
        <%= select_tag 'radius', "<option>Select radius</option><option>1/2 Mile</option><option>2 Miles</option><option>5 Miles</option><option>10 Miles</option>".html_safe %>
        </div>
        <%= submit_tag "Create", :class => "hvr-grow-shadow submit-button dim", :id => "create-button", data: {disable_with: false} %>
      <% end %>
    </div>
<!--     <div class="options">
      <div>Create</div>
   </div> -->
    <!-- <div class="join-form">
      <%= form_tag find_key_path, method: :get do %>
        <div><%= label_tag "Name:", nil, :class => "name-label" %></div>
        <div><%= text_field_tag :name %></div>
        <div><%= label_tag "Key:", nil, :class => "key-label" %></div>
        <div><%= text_field_tag :key %></div>
        <div><%= submit_tag 'Join', :class => "submit-button" %></div>
       <% end %>
    </div> -->
</div>
  <div id="map"></div>
</div>
