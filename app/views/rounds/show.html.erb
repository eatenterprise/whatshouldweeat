<div id='container'>
  <h1>round show page</h1>

  <% if session[:creator] %>
    <%= @round.key %>
  <% end %>

  <div id="secret" data-round-id="<%= @round.id %>"></div>

  <ul id="users-ul">
  <% @round.users.each do |user| %>
  <li><%= user.name %></li>
  <% end %>
  </ul>

  <%= form_tag round_restaurants_path(@round), method: :post, :id => 'new-restaurant' do %>
    <%= text_field_tag :name %>
    <%= submit_tag 'Submit', :data => {disable_with: false} %>
  <% end %>

  <ul id="restaurant-list">

  </ul>

  <% if session[:creator] %>
    <%= link_to 'Start', round_start_path(@round), id: 'start' %>
  <% end %>
</div>
